/*export function validaCadastro(nome, email, senha){
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if(emailRegex.test(email)){
        return "email inválido!";
    }
}

module.exports = { validaCadastro };*/